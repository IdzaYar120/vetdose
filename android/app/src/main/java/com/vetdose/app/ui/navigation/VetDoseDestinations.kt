package com.vetdose.app.ui.navigation

/**
 * Route names for [androidx.navigation.NavHost]. Screens are added in Stage 5;
 * for now the graph has a single placeholder "calculate" destination so the
 * navigation scaffolding itself is in place and testable.
 */
object VetDoseDestinations {
    const val CALCULATE = "calculate"
}
