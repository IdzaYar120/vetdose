<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only override: allows plain HTTP to the Android emulator's
     host-loopback alias so the app can reach `docker compose up`'s backend
     on the developer's machine without HTTPS. Never merged into release
     builds (this file lives under src/debug/, not src/main/). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
